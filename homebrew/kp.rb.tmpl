# Generated by `make release-formula` — do not edit by hand.
# Source template: homebrew/kp.rb.tmpl in the kp repo.
class Kp < Formula
  desc "A terminal tool to manage encrypted key/value pairs"
  homepage "https://github.com/simonski/kp"
  version "{{VERSION}}"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/simonski/kp/releases/download/v{{VERSION}}/kp_{{VERSION}}_darwin_arm64.tar.gz"
      sha256 "{{SHA_DARWIN_ARM64}}"
    end

    on_intel do
      url "https://github.com/simonski/kp/releases/download/v{{VERSION}}/kp_{{VERSION}}_darwin_amd64.tar.gz"
      sha256 "{{SHA_DARWIN_AMD64}}"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/simonski/kp/releases/download/v{{VERSION}}/kp_{{VERSION}}_linux_arm64.tar.gz"
      sha256 "{{SHA_LINUX_ARM64}}"
    end

    on_intel do
      url "https://github.com/simonski/kp/releases/download/v{{VERSION}}/kp_{{VERSION}}_linux_amd64.tar.gz"
      sha256 "{{SHA_LINUX_AMD64}}"
    end
  end

  def install
    bin.install "kp"
  end

  test do
    assert_match "{{VERSION}}", shell_output("#{bin}/kp version")
  end
end
